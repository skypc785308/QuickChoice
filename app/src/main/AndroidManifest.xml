<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mis.ncyu.quickchoice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!-- Permission to vibrate — recommended, allows vibration feedback on scan -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Permission to use camera — required -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Camera features — recommended -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <!--
       The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
       Google Maps Android API v2, but you must specify either coarse or fine
       location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- 設定議定要有OpenGL ES version 2才能下載本APP -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcome_view">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="登入" />
        <activity
            android:name=".sign_new"
            android:label="註冊" />
        <activity
            android:name=".My_card"
            android:label="@string/mycard" />
        <activity android:name=".news" />
        <activity
            android:name=".Add_new_card"
            android:label="選擇銀行" />
        <activity
            android:name=".Chose_card_type"
            android:label="選擇卡片類型" />
        <activity
            android:name=".key_in_card"
            android:label="輸入卡片資訊" />
        <activity
            android:name=".ForgetActivity"
            android:label="忘記密碼" />
        <activity
            android:name=".content_my_card"
            android:label="卡片資訊" />
        <activity
            android:name=".new_home2"
            android:label="主畫面"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Start_recommend"
            android:label="推薦結果" />
        <activity android:name=".Choice_recommend_type" />
        <activity
            android:name=".compute_recommend"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".get_pos" />
        <activity android:name=".compute_get_map_json" />
        <activity
            android:name=".key_in_recommend"
            android:label="輸入推薦資料" />
        <activity android:name=".compute_wating" />
        <activity android:name=".content_result"></activity>
    </application>

</manifest>